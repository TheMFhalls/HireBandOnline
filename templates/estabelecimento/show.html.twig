{% extends 'admin.html.twig' %}

{% block title %}Estabelecimento{% endblock %}

{% block body %}
    <h1>Estabelecimento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estabelecimento.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ estabelecimento.nome }}</td>
            </tr>
            <tr>
                <th>Historia</th>
                <td>{{ estabelecimento.historia }}</td>
            </tr>
            <tr>
                <th>Endereco</th>
                <td>{{ estabelecimento.endereco }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estabelecimento_index') }}">back to list</a>

    <a href="{{ path('estabelecimento_edit', {'id': estabelecimento.id}) }}">edit</a>

    {{ include('estabelecimento/_delete_form.html.twig') }}
{% endblock %}