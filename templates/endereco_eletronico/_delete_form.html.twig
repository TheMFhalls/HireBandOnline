<form method="post" action="{{ path('endereco_eletronico_delete', {'id': endereco_eletronico.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ endereco_eletronico.id) }}">
    <button class="btn">Delete</button>
</form>